== README

### Rails into project

* MVC
* one-to-many
* manay-to-many

Install:

1) Clone this repo
2) cd cookbook_app
3) bundle install
4) rake db:seed
5) rake db:migrate
6) rails s
7) go to localhost:3000
